# Copy to .env and fill in. Never commit the real .env.

# Sprout > Settings > API Access. Requires the Premium Analytics add-on.
SPROUT_API_TOKEN=

# Optional. Left blank, the server resolves it from /metadata/client on first call.
SPROUT_CUSTOMER_ID=
